\documentclass{article}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{xcolor}
\usepackage{listings}
\usepackage{hyperref}

% Theorem-like blocks (theorem, lemma, definition, example, remark) and
% section/subsection are all numbered by notetaker itself (one shared,
% hierarchical counter across every numbered label — see src/numbering.rs),
% not by LaTeX's own counters, so they're rendered as plain \par/\section*
% text with the number spliced in rather than via \newtheorem/\section.
% \proof has no number to show, so it still uses amsthm's built-in environment.

\lstset{
  basicstyle=\ttfamily\small,
  keywordstyle=\color{blue},
  commentstyle=\color{gray},
  stringstyle=\color{teal},
  showstringspaces=false,
  breaklines=true,
  frame=single
}

\title{}
\author{}
\date{}

\begin{document}
\maketitle

% NOTETAKER:CONTENT

\end{document}
